import 'package:flutter/material.dart';

class VideoByGroup extends StatelessWidget {
  const VideoByGroup({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
